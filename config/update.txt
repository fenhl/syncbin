syncbin 2.0 patch notes

All symlinks to /opt/hub/fenhl/syncbin should be changed to /opt/git/github.com/fenhl/syncbin/master
Make sure the directory infrastructure in /opt/git is up to date and that /opt/hub is removed or a symlink to /opt/git/github.com
