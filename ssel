#!/bin/zsh

if [[ $# -ge 1 ]] && [[ "$1" == '--sudo' ]]; then
    if where tac &> /dev/null; then
        sudo tac "$@" | less
    else
        sudo tail -r "$@" | less
    fi
else
    if where tac &> /dev/null; then
        tac "$@" | less
    else
        tail -r "$@" | less
    fi
fi
